{
  "name": "RoboVerse DevContainer",
  "image": "ubuntu:20.04",
  "mounts": [
    "source=${localEnv:HOME}/.ssh,target=/root/.ssh,type=bind"
  ],
  "postCreateCommand": "apt update && apt install -y git openssh-client && git config --global user.name 'Boqi Zhao' && git config --global user.email 'your_email@example.com'"
}
